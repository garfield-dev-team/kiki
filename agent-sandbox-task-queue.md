# Agent Sandbox（Manus 类产品）场景下任务队列的适用性分析

> 背景：基于《生产级任务队列（Production-Grade Job Queue）技术方案设计》的延伸思考。
> 核心问题：当用户请求过来时需要分配一个容器沙箱——任务的领取与容器沙箱分配，可以用这套任务队列管理吗？

可以，而且这个场景和租约机制契合度非常高——但有一个关键的**建模转换**要先讲清楚：**沙箱不是一个"任务"，它是一个"被租用的资源"**。任务队列天生管的是"干活然后结束"的事件流，而容器沙箱是被某个会话长期持有（几分钟到几小时）的稀缺资源。正确姿势是：用同一套队列机制，把沙箱生命周期本身建成一个"资源池队列"，把围绕它的三类事件（用户请求、容器供给、回收清理）建成任务队列。

## 一、操作映射：队列原语 ↔ 沙箱生命周期

| 队列操作 | 沙箱场景下的语义 | 为什么恰好合适 |
|---|---|---|
| `enqueue` 沙箱进池 | 预热容器进入 warm pool；`delay_ms` 天然支持"错峰预热"（预测早高峰，提前 N 分钟排预热任务） | 延迟队列白送了预热调度 |
| `reserve` | **给会话分配容器**。`ZPOPMIN` 原子弹出 → 两个用户绝不可能拿到同一个容器 | 这是沙箱平台最经典的分配 bug，队列机制直接消灭 |
| `heartbeat` | 会话存活续约。更真实的做法：**把续约挂在活动上**——agent 每次 API 调用/网关每次代理请求顺带续租，而不是裸定时器 | "用户还在干活，容器就留着" |
| `complete` | 会话正常结束 → 销毁容器（或走 reset 流程回池复用） | — |
| `fail` | 供给失败（镜像拉取失败、节点配额超限）→ 指数退避重试 → DLQ | 教科书式契合 |
| `sweep` + fencing | **僵尸容器回收器**：会话崩溃/用户关页面 → 心跳停 → vis 到期 → 强杀容器释放资源；旧会话再碰沙箱 `ERR_FENCED` | 这是省钱机制，见下文 |
| `lease_resets` 上限 → DLQ | 一个容器反复被申领又被秒退（坏镜像、启动即崩）→ 进 DLQ 待人工检查 | 毒丸防线的直接复用 |
| `release` | 会话迁移/优雅退出，容器立即回池，不白等一个 vis 周期 | — |

## 二、具体架构：一个资源池 + 三条事件队列

```
用户请求 ──enqueue(pri=会员等级)──►  qk:req       ◄── Dispatcher(bind.lua 原子配对)
                                     qk:sbx:{type}  ◄── warm pool（reserve 的就是它）
                                     qk:prov        ◄── Provisioner worker（调 K8s API 拉容器）
                                     qk:reset       ◄── 容器回收复用（sanitize 后重新入池）
```

一次请求的完整流转：

1. **请求到达网关** → `enqueue` 进 `qk:req`，priority 用会员等级，队列天然就是**等待室**（前端还可用 `ZRANK` 免费送用户一个排队位次显示）。
2. **Dispatcher 跑一个 `bind.lua`**：单脚本内原子地"池里有 warm 容器才领请求"——`ZPOPMIN` 请求 → `ZPOPMIN` 容器 → 双向 `HSET` 绑定（session_id、容器地址、**fencing token**）→ 返回网关。池空就不领，请求原地排队。这是原方案之外唯一需要新写的小脚本，解决"两个队列的会合（rendezvous）"问题。
3. **池水位低于目标** → dispatcher `enqueue` 供给任务进 `qk:prov`（带 delay 排峰谷）。Provisioner `reserve` 后调 Docker/K8s API（冷启动 2–60s，期间正常心跳），成功 `complete`，容器进入 `qk:sbx` 成为可分配资源。
4. **会话期间**：网关每 20s 心跳（vis=60s）；agent 每次 API 调用也续租。**fencing token 下发到网关**——之后该会话对沙箱的每一次 exec/代理请求都校验 token。
5. **会话结束** → `complete` → 容器进 `qk:reset`（sanitize 后重新入池）或直接销毁。
6. **agent 崩溃/用户直接关页面** → 心跳停 → 60s 后 sweep 强杀容器。此后僵尸会话的任何请求撞上网关的 token 校验 → `ERR_FENCED` → 直接返回 410。**幽灵会话从此碰不到已易主的沙箱**——fencing token 在这个场景的价值比在普通任务队列里还大，因为它直接变成了访问控制的凭据。

## 三、这个场景里原方案的哪些设计突然变得特别值钱

1. **sweep 是成本控制核心**。Manus 类产品里 agent 大量时间在"等 LLM 返回/等用户输入"，没人回收的空闲容器就是纯烧钱。"心跳停 60 秒即回收 + 复活即拒"直接决定毛利。
2. **`ERR_FENCED` 升级为安全边界**。普通队列里 fence 拒绝只是防重复执行；这里它是"旧会话不得再触碰已回收沙箱"的强制手段。
3. **两道毒丸防线都有真实对应物**：坏镜像 = 反复 fail → DLQ；启动即崩的容器 = 没人调 fail、反复超时 → `lease_resets` 上限 → DLQ。缺了第二道，坏容器会在"分配→崩溃→回收→再分配"里无限循环，每个路过它的会话都白等一次冷启动。

## 四、诚实的差距清单：队列解决不了、需要补的东西

1. **放置（placement）不归队列管**。队列回答"下一个轮到谁"，不回答"放到哪个节点"。容器有 CPU/内存/GPU 预算，需要 bin-packing。如果跑在 K8s 上，放置交给调度器，队列只编排工作流；如果是自有裸容器集群，要加一个看节点资源账本的放置组件。GPU 池这类异构资源用队列分片解决：`qk:sbx:gpu:us-east`、`qk:sbx:std:us-east`，类型即队列。
2. **缺"绝对持有上限"**。原方案只有"无心跳多久回收"，没有"总共最多持有多久"。失控 agent 挂着 GPU 容器跑 6 小时，心跳一直正常，sweep 永远不管。需要加一个 `max_hold_ms`：heartbeat/sweep 脚本里检查 `now - first_reserve_ms` 超限则走强制销毁路径。沙箱场景这个必须加。
3. **回收 ≠ 可复用**。上一用户的沙箱必须经过 reset 才能回池，否则就是数据泄漏。做法是把 reset 本身建成任务（`qk:reset`）：reset 失败进 DLQ，而不是悄悄把脏容器发给别人——用队列自己的毒丸机制保证清理流程的可靠性，这个自指很优雅。
4. **单用户公平性**。一个用户灌 500 个请求会吃光池子。准入处加每用户并发信号量（`user:{u}:active` 计数 + 会话结束递减，同样用租约语义防泄漏），或者干脆每用户独立队列 + 加权轮询 dispatch。
5. **指标语义要换**。此时"队列深度"只是冷启动等待人数，真正的容量信号是 **warm pool 占用率、冷启动率、回收速率、池水位预测**——告警线要围绕这些重设。

## 结论

**任务领取（reserve 分配工作）是原方案的直译；容器分配则是把原方案反过来用——沙箱进"资源池"当被 reserve 的对象，会话租约走 heartbeat/sweep/fencing 全套机制，队列同时管住请求排队、容器供给、回收清理三条事件流。机制全部现成，需要补的只有 `bind.lua` 配对脚本、`max_hold` 绝对上限、reset-回池流程和放置组件，这些都是在原状态机上做加法，不改地基。**
